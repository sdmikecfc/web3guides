# S6 promo assembly (rescued from session scratchpad 2026-08-22)

The lost-pipelines law: a shipped asset with no committed generator is a
one-way door. These built the S6 UPRISING launch promos
(`Documents\Doma\S6-promo\launchwars_s6_promo_169.mp4` / `_916.mp4`).

- `build_promo3.py` - the final assembler (v4b cut: ignition + title,
  PLAY FOR JUST $5 flyover, EARN REAL MONEY board, BUY REAL DOMAINS Doma
  pan, arcade cuts, 4 mech slams + war cry, Vega closer). `patch_v4.py` /
  `patch_v4b.py` record the last edits applied to it.
- `build_promo.py` / `build_promo2.py` - earlier cuts, kept for the recipe
  notes in their headers (copy-concat because filter-graph concat OOMs when
  a vercel build eats the commit charge; PIL text plates because ffmpeg
  drawtext was unreadable).
- `make_endcards.py` - the PIL end cards.

Paths inside still point at the dead scratchpad; set SP/GAMES at the top
before reuse. Game recordings live in `public/Promo Video Game/`. AI source
clips (paid Higgsfield credits): `Documents\Doma\S6-promo\`.
