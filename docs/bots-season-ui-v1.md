# Season workshop and shared game rooms — preview verification

Date: 2026-09-12. This is the preview UI behind `BOTS_SEASON_V1` and `NEXT_PUBLIC_BOTS_SEASON_V1`; it does not enable a hosted season or apply a migration.

## Implemented

- The new season has its own shell and session-only wallet sign-in. Visiting, browsing, or opening the tour does not enroll a player, grant coins, or hydrate the old garage. Collection opens the existing saved garage separately.
- Fresh visitors can build, browse, watch the trailer, or take a short illustrated tour. Returning players can reopen “Show me around.” The tour only saves its presentation version; it never resets a robot or draft.
- The build starts with the body and keeps all seven choices editable until Finish. Left and right limbs are independent. The selected body explains its special, the basic styles have plain strengths and weaknesses, and incompatible signatures remain visible instead of silently changing another part.
- Shop and build comparisons use canonical `statsV6`, GP, price, and part points. A finished robot is a reference for planning another robot. The sixteen-item shelf has no permanent inspector; details open in a native dialog. Filters stay below the shelf.
- Browser drafts contain canonical part IDs, name, and defense preference only. Wallet/server revisions and balances are not imported from browser storage. Conflicting browser and wallet drafts require a clear choice. A failed draft or Finish request retains its request ID for an identical retry.
- Season Garage renders its five exact saved builds in one shared Three.js room, reusing the approved workshop walls, floor, corkboard and numbered wheeled stands. Desktop uses one row; phones use three stands behind two. Projected stand buttons select the exact robot and open a compact clipboard containing the unchanged practice, ranked fight, defense-plan, repair and loaner actions. The daily coin calculation and trade-bonus check remain below the room.
- Recorded battles, rivalries, and standings use the actual API records. Defender replays explain that they are recorded defenses; they do not imply online presence. Direct rematches use the actual rival robot ID.
- Past-season collections expose each expired purse separately. Their build review charges for unique missing designs, reuses owned designs, and leaves the original robots intact.
- Versions 5 and 6 use the centered fight-room frame with a prominent Special area, popup details, and persistent six-room navigation. Existing demo and saved-replay routes enter the same game shell while retaining their original identity and simulation path.
- Community is a room, not a full-screen legacy drawer. Occupants load independently; successful actors remain visible when another actor fails. The helper inspects its workshop, visitors arrive, and robots make restrained gestures. Recorded activity and practice examples remain clearly labeled.
- All 135 canonical cards connect to the admitted catalogue assets. The daily shelf remains sixteen items; “All designs” adds part and tier filters. The builder offers all four tiers and keeps affordability and signature restrictions explicit. Choosing a design is free; Finish pays for the exact seven choices.
- Every part popup shows its actual rotatable model. Static shelf and saved-stand pictures use a serialized photograph queue with one temporary offscreen renderer, bounded image caching, and cancellation on hidden/unmounted items. Exact saved builds keep their asset and collision identity. Partial previews hide unchosen sockets and use the authored relaxed arm pose when no weapon is selected; invisible rig fillers never enter a saved draft.
- Exact-item practice links cover every canonical part, all six families and four tiers, independent mixed builds, and weapon signatures. Broken or incompatible links show an error instead of displaying a replacement robot. Reduced motion forces a visibly labeled steady camera. Render diagnostics appear only in development.

## Checks completed

- Full project TypeScript: `tsc --noEmit --incremental false --pretty false` passed after the UI changes.
- `scripts/bots-season-workshop-check.ts`: ten groups passed. They cover 250-coin/100-GP basic builds, combat-stat agreement, independent limbs, final-weapon preservation, incompatible signatures, safe draft parsing, partial-preview isolation, archived-design cost, exact hero-still identity, URL identity, every one of the 135 part links, all 24 family/tier examples, and explicit rejection of invalid practice links.
- Existing styled catalogue/UI domain regression: seven groups passed; older drafts and catalogue identities remain supported.
- Isolated browser checks passed at 1280×720, 390×844, and 844×390, using Chromium with reduced motion. The new shop shows sixteen complete desktop tiles and nine complete portrait-phone tiles. Short landscape keeps navigation visible with four complete shelf tiles and a scrolling shelf.
- Version-5 fight checks found the arena centered at all three sizes, with Special controls above navigation. The details dialog closes with Escape. The original style URL preserves its seed in the shared shell.
- Community reached five loaded occupants with persistent navigation and no old full-screen drawer. The fresh welcome does not intercept navigation away from a deep-linked fight.
- A development-only real-renderer fixture injected one failed actor load, restored only that actor, failed a replacement, and unmounted during five delayed replacements. Four neighbors remained visible during the initial failure. The stale replaced model disappeared, retry restored all five, and all 19 created models were disposed after unmount. No browser page errors occurred.
- The seasonal garage loaded five mixed canonical builds with a single canvas at desktop, phone-size and short-landscape viewports. Measured foot bounds matched the trolley surface within numerical precision. Deliberately failing a unique shoulder-cannon asset left the other four robots and all five controls available; retry loaded the missing robot. Selection opened the correct clipboard and Escape restored the stand control. The screenshots were inspected for visible stand labels and feet; no API mutations occurred in this intercepted test.
- Fresh welcome and tour browser checks made no POST requests. Independent choices and an unfinished draft survived room navigation.
- A fully intercepted wallet/API test exercised a failed draft save and failed Finish, then identical retries. Both retries reused the original operation ID; exactly one robot was created and one 250-coin allowance was spent in the test fixture. No old `/me`, handoff, or enlist route was called. This is mocked API verification, not a real wallet signature test.
- The Finish fixture was regenerated from the current canonical rules, and the exact finished robot loaded in its saved stand in the shared room scene. Earlier `mk6-1` proof snapshots intentionally fail validation under the later physical-leg rules; the UI did not rewrite those old proof outcomes. Versions 1–5 remain separate.
- All ten ordinary tier-two weapons had distinct loaded photographs, loaded rotatable popup models, and exact canonical practice links. Rifle, flame sword, flamethrower, and shoulder-cannon links loaded playable examples. Desktop, phone-size, and short-landscape reruns loaded actual shop images and retained sixteen, nine, and four complete tiles respectively, with no horizontal overflow or unexpected API writes.
- The complete classic build regression selected mixed independent limbs, finished one exact robot for 250, and reached the actual styled practice link. All sixteen classic shop images loaded; nine complete tiles fit the phone shelf. No browser page errors occurred in these completed checks.
- Direct legacy routes `/bots/fight/demo?seed=75&showcase=1&loadout=hammer` and `/bots/fight/demo-spar` passed desktop/phone checks with an actual canvas, six room buttons, no horizontal overflow, and a working Garage action. This check caught and fixed the ancestor legacy mobile dock intercepting the new room navigation.

Browser scripts and screenshots are in the task workspace at `C:/Users/Mike/Documents/Codex/2026-09-07/i-b/outputs/v6-ui-review`. Reports include `season-ui-checks.json`, `season-save-checks.json`, `shell-checks.json`, `legacy-shell-checks.json`, and the `classic` folder. Portable domain checks are included as source files under `scripts`.

The subsequent release checks are in `C:/Users/Mike/Documents/Codex/2026-09-07/i-b/outputs/v6-ui-release`: `room-loading-checks.json`, `season-ui-checks.json`, `season-save-checks.json`, `season-weapons-checks.json`, and `season-garage-checks.json`. Shared-garage screenshots are `garage-shared-1280x720.png`, `garage-shared-390x844.png`, `garage-shared-844x390.png`, and `garage-selected-clipboard.png`. These confirm UI behavior and asset identity; screenshots remain provisional while the art lane restores lower-tier limb covers.

## Remaining limits

- All catalogue asset connectors now work, but the latest lower-tier silhouettes are being revised after visual review. Do not treat these screenshots as final art approval. The UI has no generic old-model substitution for missing new art; an actual loading failure stays explicit and keeps the selected choices.
- The shared seasonal garage found an inactive backup-gun mount below the trolley on two shoulder-kit builds. The engine lane identified the cause and is correcting that weapon pose. The floor-contact calculation uses the real feet and must not be changed to lift a robot by its weapon.
- Hosted season migration/enrollment remains disabled until the backend rollout is approved and applied. A confirmed unavailable season is explained; anonymous welcome and shop copy do not guess when it opens. Building, instruction, and real practice actions work without a wallet.
- Real wallet signing, hosted transactional behavior, a physical mobile device, and the 60/30 FPS hardware targets were not verified by this UI lane. Headless Chromium layout checks are not a physical-mobile performance measurement.
- An eight-second provisional headless render sample used Chromium 151 with SwiftShader software rendering on Windows 10, an i5-11300H, eight logical cores, and 24 GiB RAM while other training work was running. It reported about 2 FPS at 1280×720 and 2–3 FPS at a 390×844 viewport; those conditions do not establish hardware performance. The sample did confirm centered arenas, reachable Special controls, reduced-motion camera enforcement, and persistent moved vertices. `render-sample.json` records the raw environment and samples. Final hardware performance remains untested.
- Whole-project TypeScript passed again after the final UI changes and the renderer lane's Map-iteration correction.
- No DomaReporter code, trading service, live database, or paid generation was changed by this UI work.

## Entry and navigation audit, 12 September

The Fight room now promotes the exact selected example's practice action. Tank, Speed and Ranged examples remain explicitly labeled Tier 3 practice robots; they are not starter grants. The fresh Build action opens the body choice first. The Community fallback ticket says “Practice fight,” matching its action rather than promising a saved replay.

Reading an unreadable optional classic-save JSON value no longer overrides a separate seen-tour choice. The existing value is left untouched. In the isolated browser check, the seasonal draft also stayed byte-for-byte unchanged after returning from other rooms.

`audit-season-entry.cjs` in the task workspace intercepts game API requests. The failure case loaded all five Community actors with persistent desktop and portrait navigation, while displaying the loading failure and a practice action. The source still distinguishes unavailable data from an empty successful response. A deep-linked Tier 1 practice setup finished loading inside the shared shell without a welcome interception. Full-project strict TypeScript passed after the contained UI changes. Evidence is in `outputs/v6-ui-release/entry-audit.json` and the `entry-audit-*.png` screenshots. These screenshots verify interface behavior; they do not replace the ongoing robot-art review or hardware performance checks.
